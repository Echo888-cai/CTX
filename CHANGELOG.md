# Changelog

## 0.2.1

- Standalone macOS app: Dock, dashboard window, and menu-bar tray (`ctx app --install-app`, or `CTX-macos-*.zip` on GitHub Releases).
- Dashboard cache-hit rate, with a seamless flowing-text shine on the percentage.
- Model merge in telemetry / ledger pricing.
- Release assets: CLI tarballs (macOS, Linux, Windows) plus the Mac app zip.
